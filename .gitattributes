# Chuẩn hóa thống nhất cuối dòng: blob và working tree đều LF, ghi đè khác biệt core.autocrlf giữa các máy.
# Tất cả blob trong repo vốn đã được lưu bằng LF; trên Windows, các công cụ trộn lẫn cuối dòng từng tạo ra rất nhiều thay đổi ảo.
* text=auto eol=lf